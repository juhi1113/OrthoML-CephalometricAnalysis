Installation steps:

1) Install VS Code, make sure to set the Python interpreter env properly 
2) pip install flask

In any case, getting error, do follow below mentioned steps:

1)Firstly run python -m venv myenv and then run .\myenv\Scripts\Activate

2) & path/to/your/folder

3) pip install flask
 
4) pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu113

5) to list it out run pip list

6) Lastly, restart your VS Code and try to run the program i.e. App.js

7) After running the code in output there would be a link like this 'http://127.0.0.1:5000/' Copy-paste that in any search engine you have/directly open.